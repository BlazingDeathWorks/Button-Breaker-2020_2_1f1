using UnityEngine;
using UnityEngine.UI;

public class Text_Ref : MonoBehaviour
{
    public Text Actual_textref;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
